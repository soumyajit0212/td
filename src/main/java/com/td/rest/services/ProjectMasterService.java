/*
 * Created on 2025-07-14 ( 12:36:35 )
 * Generated by Telosys ( https://www.telosys.org/ ) version 4.1.0
 */
package com.td.rest.services;

import java.util.List;
import java.util.Optional;

import java.math.BigDecimal;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;

import com.td.persistence.entities.ProjectMaster;
import com.td.persistence.repositories.ProjectMasterRepository;
import com.td.rest.dto.ProjectMasterDTO;
import com.td.rest.services.commons.GenericService;

/**
 * REST service for entity "ProjectMaster" <br>
 * 
 * This service provides all the necessary operations required by the REST controller <br>
 * 
 * @author Telosys
 *
 */
@Service
public class ProjectMasterService extends GenericService<ProjectMaster, ProjectMasterDTO> {

	private static final Logger logger = LoggerFactory.getLogger(ProjectMasterService.class);

	private final ProjectMasterRepository repository; // injected by constructor

	/**
	 * Constructor (usable for Dependency Injection)
	 * 
	 * @param repository the repository to be injected
	 */
	public ProjectMasterService(ProjectMasterRepository repository) {
		super(ProjectMaster.class, ProjectMasterDTO.class);
		this.repository = repository;
	}
	
	/**
	 * Returns the entity ID object from the given DTO
	 *
	 * @param dto
	 * @return
	 */
	private BigDecimal getEntityId(ProjectMasterDTO dto) {
		return dto.getProjectId();
	}

	/**
	 * Finds all occurrences of the entity
	 *
	 * @return
	 */
	public List<ProjectMasterDTO> findAll() {
		logger.debug("findAll()");
		Iterable<ProjectMaster> all = repository.findAll();
		return entityListToDtoList(all);
	}

	/**
	 * Finds the entity identified by the given PK
	 *
	 * @param projectId 
	 * @return the entity or null if not found
	 */
	public ProjectMasterDTO findById(BigDecimal projectId) {
		BigDecimal entityId = projectId;
		logger.debug("findById({})", entityId);
		Optional<ProjectMaster> optionalEntity = repository.findById(entityId);
		return entityToDto(optionalEntity);
	}

	/**
	 * Saves the given entity with the given PK <br>
	 * "UPSERT" operation (updated if it exists or created if it does not exist)
	 *
	 * @param projectId 
	 * @param dto 
	 */
	public void save(BigDecimal projectId, ProjectMasterDTO dto) {
		BigDecimal entityId = projectId;
		logger.debug("save({},{})", entityId, dto);
		// force PK in DTO (just to be sure to conform with the given PK) 
		dto.setProjectId(projectId);
		repository.save(dtoToEntity(dto));
	}

	/**
	 * Updates the given entity if it exists
	 *
	 * @param dto
	 * @return true if updated, false if not found
	 */
	public boolean update(ProjectMasterDTO dto) {
		logger.debug("update({})", dto);
		if (repository.existsById(getEntityId(dto))) {
			repository.save(dtoToEntity(dto));
			return true; // find and updated
		} else {
			return false; // not found (not updated)
		}
	}

	/**
	 * Updates partially the given entity if it exists
	 *
	 * @param projectId 
	 * @param dto
	 * @return true if updated, false if not found
	 */
	public boolean partialUpdate(BigDecimal projectId, ProjectMasterDTO dto) {
		BigDecimal entityId = projectId;
		logger.debug("partialUpdate({}, {})", entityId, dto);
		Optional<ProjectMaster> optionalEntity = repository.findById(entityId);
		if (optionalEntity.isPresent()) {
			ProjectMaster entity = optionalEntity.get();
			// implement here the partial update
			// entity.setXxx(dto.getXxx());
			// etc ...
			repository.save(entity);
			return true; // find and updated
		} else {
			return false; // not found (not updated)
		}
	}

	/**
	 * Creates the given entity
	 *
	 * @param dto
	 * @return true if created, false if already exists
	 */
	public boolean create(ProjectMasterDTO dto) {
		logger.debug("create({})", dto);
		// auto-generated Primary Key
		repository.save(dtoToEntity(dto));
		return true; // always created
	}

	/**
	 * Deletes an entity by its PK
	 *
	 * @param projectId 
	 * @return true if deleted, false if not found
	 */
	public boolean deleteById(BigDecimal projectId) {
		BigDecimal entityId = projectId;
		logger.debug("deleteById({})", entityId);
		if (repository.existsById(entityId)) {
			repository.deleteById(entityId);
			return true; // find and deleted
		} else {
			return false; // not found (not deleted)
		}
	}

	// -----------------------------------------------------------------------------------------
	// Specific "finders"
	// -----------------------------------------------------------------------------------------
/***
	public List<ProjectMasterDTO> findByTitle(String title) {
		logger.debug("findByTitle({})", title);
		// List<ProjectMaster> list = repository.findByTitle(title);
		List<ProjectMaster> list = repository.findByTitleContaining(title);
		return entityListToDtoList(list);
	}

	public List<ProjectMasterDTO> findByPrice(BigDecimal price) {
		logger.debug("findByPrice({})", price);
		// List<ProjectMaster> list = repository.findByTitle(title);
		List<ProjectMaster> list = repository.findByPrice(price);
		return entityListToDtoList(list);
	}

	public List<ProjectMasterDTO> findByTitleAndPrice(String title, BigDecimal price) {
		logger.debug("findByTitleAndPrice({}, {})", title, price);
		List<ProjectMaster> list = repository.findByTitleContainingAndPrice(title, price);
		return entityListToDtoList(list);
	}
***/
}
