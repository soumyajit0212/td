/*
 * Created on 2025-07-14 ( 12:36:35 )
 * Generated by Telosys ( https://www.telosys.org/ ) version 4.1.0
 */
package com.td.rest.services;

import java.util.List;
import java.util.Optional;

import java.math.BigDecimal;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;

import com.td.persistence.entities.SubTasks;
import com.td.persistence.repositories.SubTasksRepository;
import com.td.rest.dto.SubTasksDTO;
import com.td.rest.services.commons.GenericService;

/**
 * REST service for entity "SubTasks" <br>
 * 
 * This service provides all the necessary operations required by the REST controller <br>
 * 
 * @author Telosys
 *
 */
@Service
public class SubTasksService extends GenericService<SubTasks, SubTasksDTO> {

	private static final Logger logger = LoggerFactory.getLogger(SubTasksService.class);

	private final SubTasksRepository repository; // injected by constructor

	/**
	 * Constructor (usable for Dependency Injection)
	 * 
	 * @param repository the repository to be injected
	 */
	public SubTasksService(SubTasksRepository repository) {
		super(SubTasks.class, SubTasksDTO.class);
		this.repository = repository;
	}
	
	/**
	 * Returns the entity ID object from the given DTO
	 *
	 * @param dto
	 * @return
	 */
	private BigDecimal getEntityId(SubTasksDTO dto) {
		return dto.getSubtaskId();
	}

	/**
	 * Finds all occurrences of the entity
	 *
	 * @return
	 */
	public List<SubTasksDTO> findAll() {
		logger.debug("findAll()");
		Iterable<SubTasks> all = repository.findAll();
		return entityListToDtoList(all);
	}

	/**
	 * Finds the entity identified by the given PK
	 *
	 * @param subtaskId 
	 * @return the entity or null if not found
	 */
	public SubTasksDTO findById(BigDecimal subtaskId) {
		BigDecimal entityId = subtaskId;
		logger.debug("findById({})", entityId);
		Optional<SubTasks> optionalEntity = repository.findById(entityId);
		return entityToDto(optionalEntity);
	}

	/**
	 * Saves the given entity with the given PK <br>
	 * "UPSERT" operation (updated if it exists or created if it does not exist)
	 *
	 * @param subtaskId 
	 * @param dto 
	 */
	public void save(BigDecimal subtaskId, SubTasksDTO dto) {
		BigDecimal entityId = subtaskId;
		logger.debug("save({},{})", entityId, dto);
		// force PK in DTO (just to be sure to conform with the given PK) 
		dto.setSubtaskId(subtaskId);
		repository.save(dtoToEntity(dto));
	}

	/**
	 * Updates the given entity if it exists
	 *
	 * @param dto
	 * @return true if updated, false if not found
	 */
	public boolean update(SubTasksDTO dto) {
		logger.debug("update({})", dto);
		if (repository.existsById(getEntityId(dto))) {
			repository.save(dtoToEntity(dto));
			return true; // find and updated
		} else {
			return false; // not found (not updated)
		}
	}

	/**
	 * Updates partially the given entity if it exists
	 *
	 * @param subtaskId 
	 * @param dto
	 * @return true if updated, false if not found
	 */
	public boolean partialUpdate(BigDecimal subtaskId, SubTasksDTO dto) {
		BigDecimal entityId = subtaskId;
		logger.debug("partialUpdate({}, {})", entityId, dto);
		Optional<SubTasks> optionalEntity = repository.findById(entityId);
		if (optionalEntity.isPresent()) {
			SubTasks entity = optionalEntity.get();
			// implement here the partial update
			// entity.setXxx(dto.getXxx());
			// etc ...
			repository.save(entity);
			return true; // find and updated
		} else {
			return false; // not found (not updated)
		}
	}

	/**
	 * Creates the given entity
	 *
	 * @param dto
	 * @return true if created, false if already exists
	 */
	public boolean create(SubTasksDTO dto) {
		logger.debug("create({})", dto);
		// auto-generated Primary Key
		repository.save(dtoToEntity(dto));
		return true; // always created
	}

	/**
	 * Deletes an entity by its PK
	 *
	 * @param subtaskId 
	 * @return true if deleted, false if not found
	 */
	public boolean deleteById(BigDecimal subtaskId) {
		BigDecimal entityId = subtaskId;
		logger.debug("deleteById({})", entityId);
		if (repository.existsById(entityId)) {
			repository.deleteById(entityId);
			return true; // find and deleted
		} else {
			return false; // not found (not deleted)
		}
	}

	// -----------------------------------------------------------------------------------------
	// Specific "finders"
	// -----------------------------------------------------------------------------------------
/***
	public List<SubTasksDTO> findByTitle(String title) {
		logger.debug("findByTitle({})", title);
		// List<SubTasks> list = repository.findByTitle(title);
		List<SubTasks> list = repository.findByTitleContaining(title);
		return entityListToDtoList(list);
	}

	public List<SubTasksDTO> findByPrice(BigDecimal price) {
		logger.debug("findByPrice({})", price);
		// List<SubTasks> list = repository.findByTitle(title);
		List<SubTasks> list = repository.findByPrice(price);
		return entityListToDtoList(list);
	}

	public List<SubTasksDTO> findByTitleAndPrice(String title, BigDecimal price) {
		logger.debug("findByTitleAndPrice({}, {})", title, price);
		List<SubTasks> list = repository.findByTitleContainingAndPrice(title, price);
		return entityListToDtoList(list);
	}
***/
}
