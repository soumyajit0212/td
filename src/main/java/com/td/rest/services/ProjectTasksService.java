/*
 * Created on 2025-07-14 ( 12:36:35 )
 * Generated by Telosys ( https://www.telosys.org/ ) version 4.1.0
 */
package com.td.rest.services;

import java.util.List;
import java.util.Optional;

import java.math.BigDecimal;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;

import com.td.persistence.entities.ProjectTasks;
import com.td.persistence.repositories.ProjectTasksRepository;
import com.td.rest.dto.ProjectTasksDTO;
import com.td.rest.services.commons.GenericService;

/**
 * REST service for entity "ProjectTasks" <br>
 * 
 * This service provides all the necessary operations required by the REST controller <br>
 * 
 * @author Telosys
 *
 */
@Service
public class ProjectTasksService extends GenericService<ProjectTasks, ProjectTasksDTO> {

	private static final Logger logger = LoggerFactory.getLogger(ProjectTasksService.class);

	private final ProjectTasksRepository repository; // injected by constructor

	/**
	 * Constructor (usable for Dependency Injection)
	 * 
	 * @param repository the repository to be injected
	 */
	public ProjectTasksService(ProjectTasksRepository repository) {
		super(ProjectTasks.class, ProjectTasksDTO.class);
		this.repository = repository;
	}
	
	/**
	 * Returns the entity ID object from the given DTO
	 *
	 * @param dto
	 * @return
	 */
	private BigDecimal getEntityId(ProjectTasksDTO dto) {
		return dto.getTaskId();
	}

	/**
	 * Finds all occurrences of the entity
	 *
	 * @return
	 */
	public List<ProjectTasksDTO> findAll() {
		logger.debug("findAll()");
		Iterable<ProjectTasks> all = repository.findAll();
		return entityListToDtoList(all);
	}

	/**
	 * Finds the entity identified by the given PK
	 *
	 * @param taskId 
	 * @return the entity or null if not found
	 */
	public ProjectTasksDTO findById(BigDecimal taskId) {
		BigDecimal entityId = taskId;
		logger.debug("findById({})", entityId);
		Optional<ProjectTasks> optionalEntity = repository.findById(entityId);
		return entityToDto(optionalEntity);
	}

	/**
	 * Saves the given entity with the given PK <br>
	 * "UPSERT" operation (updated if it exists or created if it does not exist)
	 *
	 * @param taskId 
	 * @param dto 
	 */
	public void save(BigDecimal taskId, ProjectTasksDTO dto) {
		BigDecimal entityId = taskId;
		logger.debug("save({},{})", entityId, dto);
		// force PK in DTO (just to be sure to conform with the given PK) 
		dto.setTaskId(taskId);
		repository.save(dtoToEntity(dto));
	}

	/**
	 * Updates the given entity if it exists
	 *
	 * @param dto
	 * @return true if updated, false if not found
	 */
	public boolean update(ProjectTasksDTO dto) {
		logger.debug("update({})", dto);
		if (repository.existsById(getEntityId(dto))) {
			repository.save(dtoToEntity(dto));
			return true; // find and updated
		} else {
			return false; // not found (not updated)
		}
	}

	/**
	 * Updates partially the given entity if it exists
	 *
	 * @param taskId 
	 * @param dto
	 * @return true if updated, false if not found
	 */
	public boolean partialUpdate(BigDecimal taskId, ProjectTasksDTO dto) {
		BigDecimal entityId = taskId;
		logger.debug("partialUpdate({}, {})", entityId, dto);
		Optional<ProjectTasks> optionalEntity = repository.findById(entityId);
		if (optionalEntity.isPresent()) {
			ProjectTasks entity = optionalEntity.get();
			// implement here the partial update
			// entity.setXxx(dto.getXxx());
			// etc ...
			repository.save(entity);
			return true; // find and updated
		} else {
			return false; // not found (not updated)
		}
	}

	/**
	 * Creates the given entity
	 *
	 * @param dto
	 * @return true if created, false if already exists
	 */
	public boolean create(ProjectTasksDTO dto) {
		logger.debug("create({})", dto);
		// auto-generated Primary Key
		repository.save(dtoToEntity(dto));
		return true; // always created
	}

	/**
	 * Deletes an entity by its PK
	 *
	 * @param taskId 
	 * @return true if deleted, false if not found
	 */
	public boolean deleteById(BigDecimal taskId) {
		BigDecimal entityId = taskId;
		logger.debug("deleteById({})", entityId);
		if (repository.existsById(entityId)) {
			repository.deleteById(entityId);
			return true; // find and deleted
		} else {
			return false; // not found (not deleted)
		}
	}

	// -----------------------------------------------------------------------------------------
	// Specific "finders"
	// -----------------------------------------------------------------------------------------
/***
	public List<ProjectTasksDTO> findByTitle(String title) {
		logger.debug("findByTitle({})", title);
		// List<ProjectTasks> list = repository.findByTitle(title);
		List<ProjectTasks> list = repository.findByTitleContaining(title);
		return entityListToDtoList(list);
	}

	public List<ProjectTasksDTO> findByPrice(BigDecimal price) {
		logger.debug("findByPrice({})", price);
		// List<ProjectTasks> list = repository.findByTitle(title);
		List<ProjectTasks> list = repository.findByPrice(price);
		return entityListToDtoList(list);
	}

	public List<ProjectTasksDTO> findByTitleAndPrice(String title, BigDecimal price) {
		logger.debug("findByTitleAndPrice({}, {})", title, price);
		List<ProjectTasks> list = repository.findByTitleContainingAndPrice(title, price);
		return entityListToDtoList(list);
	}
***/
}
